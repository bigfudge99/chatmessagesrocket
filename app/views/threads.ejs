<!DOCTYPE html>
<html>
<head>
    <style>
        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        td, th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #dddddd;
        }
    </style>
</head>
<body>

<h2>Threads from <%= startDay %> till <%= endDay %></h2>

<table>
    <tr>
        <th>S.No.</th>
        <th>Thread Count</th>
        <th>Message</th>
        <th>Date</th>
    </tr>
    <% for (var i = 0; i < data.length; i++) { %>
    <tr>
        <td><%= data[i].id %></td>
        <td><%= data[i].count %></td>
        <td><%= data[i].message %></td>
        <td><%= data[i].date %></td>
    </tr>
    <% } %>
</table>

</body>
</html>
