<!DOCTYPE html>
<html>
<head>
    <style>
        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        td, th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #dddddd;
        }
    </style>
</head>
<body>

<h2>Table For Users With Message <%- msg %> between <%= startDay %> and <%= endDay %></h2>

<table>
    <tr>
        <th>S.No.</th>
        <th>FROM</th>
        <th>TO</th>
        <th>Message</th>
    </tr>
    <% for (var i = 0; i < data.length; i++) { %>
    <tr>
        <td><%= data[i].id %></td>
        <td><%= data[i].from %></td>
        <td><%= data[i].to %></td>
        <td><%= data[i].message %></td>
    </tr>
    <% } %>
</table>

</body>
</html>
